# Required metadata
sonar.projectKey=de.mappedJS
sonar.projectName=MappedJS
sonar.projectVersion=0.1

# Comma-separated paths to directories with sources (required)
sonar.sources=plugin/src/js

# Language
sonar.language=js
